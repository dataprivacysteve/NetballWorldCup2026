# dependencies
node_modules/

# secrets — never commit the filled-in env
.env

# local certificates — generated per machine, never committed
infra/caddy/certs/*.pem

# build output
dist/
.next/
.next.stale-*/
.next.partial-*/
build/

# logs / os cruft
*.log
.DS_Store

# generated inspection/rendering artifacts
tmp/
.diagram-design

# local recovery/evidence archives — retain outside source control
backups/
.local-backups/
Netball_2026_Qualifiers_Logos.zip
